/* Complete test for for loop.  prints out value at end. */

void main() begin
	begin
		var x : int;

		var i : int := 0 ;
		for i := 0 to 9 do
			x := x + 10;
		enddo;
		
		printi(x); 
		printi(100); /*what the result should be.*/
	end;
end;
